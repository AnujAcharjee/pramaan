# STRATEGY:
# build docker image each time in Git Actions and push image in Docker Hub
# copy the docker-compose and Caddyfile in EC2 as we need them to run Image (they are not in Image)
# then pull image in EC2, then run
# so we don't need git in EC2 and no docker build... reduces instance load

name: Deploy App to EC2

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
      - 'sdk/**'
      - '**.md'
      - '.gitignore'
      - '.dockerignore'
  workflow_dispatch:

jobs:
  deploy:
    if: "!contains(github.event.head_commit.message, '[skip-deploy]') && !contains(github.event.head_commit.message, '[no-deploy]')"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # Login Docker Hub
      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKER_ACCESS_TOKEN }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      # Build Image + tag
      - name: Build Docker Image
        run: |
          docker build \
          -t ${{ secrets.DOCKER_USERNAME }}/pramaan:latest \
          -t ${{ secrets.DOCKER_USERNAME }}/pramaan:${{ github.sha }} \
          .

      # Push Image
      - name: Push Image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/pramaan:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/pramaan:${{ github.sha }}

      # Setup EC2 directory
      - name: Setup EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_SSH_HOST }}
          username: ${{ secrets.EC2_SSH_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            sudo mkdir -p /srv/pramaan
            sudo chown -R ubuntu:ubuntu /srv/pramaan

      # Copy files to EC2
      - name: Copy files to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_SSH_HOST }}
          username: ${{ secrets.EC2_SSH_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "docker-compose.yml,Caddyfile"
          target: "/srv/pramaan"

      # Deploy in EC2
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_SSH_HOST }}
          username: ${{ secrets.EC2_SSH_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            cd /srv/pramaan

            cat <<'EOF' > .env
            # Server Environment
            NODE_ENV=production
            PORT=8080
            RUN_SEEDS=false

            # Application Configuration
            APP_NAME=pramaan
            APP_DOMAIN=pramaan.anujacharjee.com
            APP_DOC_URL=https://github.com/AnujAcharjee/pramaan/blob/main/readme.md
            AUTH_ISSUER=https://pramaan.anujacharjee.com
            EMAIL_FROM=no-reply@pramaan.anujacharjee.com

            # Session & Token Lifespans (seconds)
            ACTIVE_SESSION_EX=900
            IDENTITY_SESSION_EX=15552000
            EMAIL_VERIFICATION_TOKEN_EX=86400
            SIGN_VERIFICATION_TOKEN_EX=86400
            SIGN_IN_FAIL_COUNT_EX=86400
            RESET_PASSWORD_EX=3600

            # OAuth Lifespans
            AUTH_CODE_EX=300
            AUTH_TOKENS_EX=10m
            AUTH_REQUEST_EX=300

            # Security & Rate Limits
            MAX_SIGNIN_FAILURES=20
            SIGNIN_LOCK_UNTIL=21600

            # Cache Lifespans
            CLIENT_CACHE_EX=900
            PROFILE_CACHE_EX=900

            # DNS TXT Verification Resolvers
            PRAMAAN_DNS_SERVERS=1.1.1.1,8.8.8.8

            # Sensitive Secrets (from GitHub Secrets)
            NEON_PG_DATABASE_URL=${{ secrets.NEON_PG_DATABASE_URL }}
            COOKIE_SECRET=${{ secrets.COOKIE_SECRET }}
            REDIS_URL=${{ secrets.REDIS_URL }}
            RESEND_API_KEY=${{ secrets.RESEND_API_KEY }}
            KEY_ENC_SECRET=${{ secrets.KEY_ENC_SECRET }}
            CLIENT_SECRET_KEY=${{ secrets.CLIENT_SECRET_KEY }}
            LOGTAIL_SOURCE_TOKEN=${{ secrets.LOGTAIL_SOURCE_TOKEN }}

            INIT_ADMIN_EMAIL=${{ secrets.INIT_ADMIN_EMAIL }}
            INIT_ADMIN_PASSWORD=${{ secrets.INIT_ADMIN_PASSWORD }}

            CLOUDINARY_CLOUD_NAME=${{ secrets.CLOUDINARY_CLOUD_NAME }}
            CLOUDINARY_API_KEY=${{ secrets.CLOUDINARY_API_KEY }}
            CLOUDINARY_API_SECRET=${{ secrets.CLOUDINARY_API_SECRET }}
            EOF

            docker compose --project-name pramaan pull
            docker compose --project-name pramaan up -d --remove-orphans

            docker image prune -f
